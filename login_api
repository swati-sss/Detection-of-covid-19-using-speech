import Foundation
import os.log

//import compass_sdk_ios

class ViewController: UIViewController {
    private let log = OSLog(subsystem: Bundle.main.bundleIdentifier ?? "com.compass.map", category: "CompassViewController")
    static var shared = ViewController()
    var compassViewList: [CompassView] = []

    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        super.init(nibName: nibNameOrNil, bundle: nibBundleOrNil)
        os_log("ViewController: init called (nibName=%{public}@)", log: log, String(describing: nibNameOrNil))
    }

    required init?(coder: NSCoder) {
        os_log("ViewController: init?(coder:) called", log: log)
        super.init(coder: coder)
    }
}
